# These binary files (as apposed to text) which are not good for git's compression
Assets/
# All of these files are in the project already so no need to have them twice.
Build/
# These are mainly binary files
Plans/